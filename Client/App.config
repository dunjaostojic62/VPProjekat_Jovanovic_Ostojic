<?xml version="1.0" encoding="utf-8" ?>
<configuration>
	<startup>
		<supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.7.2" />
	</startup>

	<appSettings>
		<!-- Putanja do CSV fajla. Stavi fajl u Client/bin/Debug/. -->
		<add key="csvFilePath" value="time_series_15min_singleindex.csv" />

		<!-- Izabrana zemlja (Zadatak 5: DE, AT, BE, HU ili NL) -->
		<add key="countryCode" value="DE" />

		<!-- Izabran dan u formatu YYYY-MM-DD -->
		<add key="selectedDate" value="2015-01-15" />

		<!-- N uzoraka po batch-u (Zadatak 1).
             16 = 4 sata podataka pri 15-minutnoj rezoluciji. -->
		<add key="batchSize" value="16" />

		<!-- Fajl za odbacene/problematicne redove (Zadatak 5). -->
		<add key="rejectedFilePath" value="rejected_client.csv" />
	</appSettings>

	<system.serviceModel>
		<client>
			<!-- name="LoadEndpoint" - klijent koristi ovo ime kada poziva
                 new ChannelFactory<ILoadService>("LoadEndpoint").
                 address mora biti ISTI kao baseAddress na serveru. -->
			<endpoint name="LoadEndpoint"
                      address="net.tcp://localhost:9000/LoadService"
                      binding="netTcpBinding"
                      bindingConfiguration="bigBinding"
                      contract="Common.ILoadService" />
		</client>

		<bindings>
			<netTcpBinding>
				<binding name="bigBinding"
                         maxReceivedMessageSize="2147483647"
                         maxBufferPoolSize="2147483647"
                         openTimeout="00:01:00"
                         closeTimeout="00:01:00"
                         sendTimeout="00:10:00"
                         receiveTimeout="00:10:00">
					<readerQuotas maxArrayLength="2147483647"
                                  maxStringContentLength="2147483647"
                                  maxDepth="64" />
				</binding>
			</netTcpBinding>
		</bindings>
	</system.serviceModel>
</configuration>